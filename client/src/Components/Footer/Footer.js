import React from "react";

const Footer = () => (
  <nav className="navbar navbar-expand-lg navbar-dark bg-primary">
    <a className="navbar-brand">The Real Doctors</a>
  </nav>
);

export default Footer;
